singleServerConfig:
  address: "redis://${REDIS_HOST}:${REDIS_PORT}"
  password: "${REDIS_PASSWORD}"
  connectTimeout: 3000
  timeout: 5000
  retryAttempts: 1
  retryInterval: 1000
  pingConnectionInterval: 30000
  keepAlive: true
  tcpNoDelay: true
